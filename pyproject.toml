[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fauxlogger"
version = "0.1.4"

[tool.pytest.ini_options]
addopts = "-s"

[project.optional-dependencies]
test = [
    "pytest>=9.0",
]